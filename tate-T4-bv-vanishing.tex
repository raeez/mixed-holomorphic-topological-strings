\subsection*{BV-cohomology vanishing and the unreduced cotangent lift}

The BV operator \(\Delta=Q+\{I_0,-\}\) of degree \(+1\) acts on
\(\mathcal O_{\mathrm{loc}}(\mathcal E_w)\) for the mixed geometry
\(X=\R^2_{\mathrm{top}}\times\widehat{\C^2}_{0,\mathrm{hol}}\) with
brane \(L=\R_{\mathrm{brane}}\times\{0\}\times\{p\}\) and ordinary
\(\lie{gl}_N\) gauge data.  An endpoint-admissible coefficient module
for Problem~\ref{prob:formal-factorization-center} exists if and only
if the four-component class
\[
  \mathfrak o_{\mathrm{BV,end}}
    =\bigl(\mathfrak o_{\mathrm{Cas}},\
            \mathfrak o_{\mathrm{def}},\
            \mathfrak o_{\mathrm{RG}},\
            \mathfrak o_{\mathrm{QME}}\bigr)
\]
of Definition~\ref{defn:bv-endpoint-obstruction-tuple} vanishes
(Proposition~\ref{prop:bv-endpoint-obstruction-tuple}): the
strict-pair Casimir cokernel
\(\mathfrak o_{\mathrm{Cas}}\)
(\ref{lem:tate-casimir-obstruction}), the brane-line propagator
extension obstruction \(\mathfrak o_{\mathrm{def}}\)
(\ref{thm:hadamard-mittag-leffler}~(2)), the strict-window
Mittag--Leffler preservation class \(\mathfrak o_{\mathrm{RG}}\)
(\ref{defn:strict-ml-tate-habitat}), and the local-functional QME
anomaly class \(\mathfrak o_{\mathrm{QME}}\in
H^1(\mathcal O_{\mathrm{loc}}(\mathcal E_w),Q+\{I_0,-\})\) of
Problem~\ref{prob:weighted-rg-locality}.

The BV grading is the cotangent-coordinate grading of
\ref{def:app-unreduced-bv-degrees} together with the signs of
\ref{def:app-sign-hamiltonian-cyclic},
\ref{cor:app-sign-universal-dual-normalization}: \(|\Delta|=+1\), the
reduced boundary \(P_0\)-bracket has degree \(0\), the Schouten
bracket has degree \(-1\).

The closed BCOV vanishing of \cite{costello-li-quantum-bcov} and the
open-closed extension of \cite{costello-li-open-closed-bcov} differ
from the present geometry along five independent axes: compact Hodge
geometry against the formal jet \(\widehat{\C^2}_0\); pure
holomorphic-volume kinematics \(\bar\partial+t\,\partial_\Omega\)
against mixed \(D=d_{\R^2}+\bar\partial_{\C^2}\); empty bulk against
the transverse line defect \(L\); the
\(\lie{gl}(N\mid N)\) supertrace cancellation
\(\mathrm{str}(I)=N-N=0\) against the ordinary
\(\lie{gl}_N\) scalar contact \(\hbar N[\bar c]\) of
\ref{thm:app-scalar-contact-qme-class}; the worldsheet \(t\)-Tate
direction against the coefficient-system Tate direction
\(\widehat{\mathfrak h}\widehat\otimes
\widehat{\mathfrak h}^{\vee}_{\mathrm{cont}}\) carrying
\(\mathfrak o_{\mathrm{Cas}}\).  Each axis names a distinct uncontrolled
component of \(\mathfrak o_{\mathrm{BV,end}}\) under the cited
hypotheses (Theorem~\ref{thm:costello-li-no-coefficient-endpoint}); the
data needed to close them is enumerated in
Theorem~\ref{thm:t4-four-missing}.  The chain-level primitive shadow
\ref{thm:chain-level-primitive-projection} closes the classical
indecomposable shadow with no analytic input, inside the admissible
Tate envelope of \ref{thm:tate-bar-cobar-quillen}.

\subsection*{BV degree convention and Costello renormalization datum}

\begin{rmk}[BV grading and \(\Delta\) for the brane-defect endpoint]
\label{rmk:t4-bv-degree-recall}
  The BV grading is fixed by
  \ref{def:app-unreduced-bv-degrees}: \(|\phi_1|=|\phi_2|=0\),
  \(|\psi=A^*|=-1\), \(Q\psi=[\phi_1,\phi_2]\), \(|Q|=+1\); a
  Hamiltonian generator \(H_f\) has degree \(0\), a shifted cotangent
  generator \(\eta_\rho=\rho[1]\) is odd of BV degree \(-1\), and the
  CE cotangent--coordinate grading is \(|c_f|=+1\), \(|u_\rho|=0\).  In
  this convention the BV operator
  \[
    \Delta=Q+\{I_0,-\}
  \]
  acting on \(\mathcal O_{\mathrm{loc}}(\mathcal E_w)\) has degree
  \(+1\), and a quantum master equation \emph{anomaly representative}
  is a degree-\(+1\) cocycle in
  \(\bigl(\mathcal O_{\mathrm{loc}}(\mathcal E_w),\Delta\bigr)\).  The
  Schouten/cotangent--coordinate antibracket has degree \(-1\); the
  reduced Hamiltonian boundary \(P_0\)-bracket has degree \(0\).  The
  scalar Hamiltonian cocycle
  \(\omega(z_1,z_2)=1\) of \ref{lem:app-sign-moyal-coefficients}
  controls the central contribution to the symbol, and the local
  Poisson sign convention \(\{z_1,z_2\}=1\) is the one of
  \ref{def:app-sign-hamiltonian-cyclic}.
\end{rmk}

\begin{rmk}[Costello (2011) heat-kernel renormalization datum invoked]
\label{rmk:t4-costello-renorm-datum}
  The endpoint comparison invokes the renormalization framework of
  \cite{costello-renormalization}*{Ch.~2 \S2, Ch.~2 \S6, Ch.~5 \S6}: a
  parametrix
  \(P_{\epsilon,L}=\int_\epsilon^Lds\,K_s\) for a generalized
  Laplacian, the homotopy RG flow
  \[
    W(P_{\epsilon,L},I)
      =\hbar\log\!\left(
        \exp(\hbar\partial_{P_{\epsilon,L}})\exp(I/\hbar)
      \right)
      \;=\;
      \sum_{\Gamma\text{ connected}}
        \frac{\hbar^{\mathrm{loops}(\Gamma)}}
             {\lvert\mathrm{Aut}(\Gamma)\rvert}
        W_\Gamma(P_{\epsilon,L},I),
  \]
  the scale-zero limit
  \(\lim_{\epsilon\to0}W(P_{\epsilon,L},I)\) constructed by counterterm
  subtraction, and the QME
  \(QI+\hbar\Delta_LI+\tfrac12\{I,I\}_L=0\) at scale \(L\).  In the
  present brane-defect geometry the propagator factorizes (when it
  exists) as
  \(P^w_{\epsilon,L}=P^{\mathrm{base}}_{\epsilon,L}
  \widehat\otimes C_{\mathfrak h\oplus\mathfrak h^\vee[1],w}\)
  (\ref{thm:hadamard-mittag-leffler}); the bulk Hadamard parametrix is
  taken from \cite{costello-renormalization}*{Ch.~3} on
  \(\R^2_{\mathrm{top}}\times\C^2_{\mathrm{hol}}\) and the coefficient
  factor is the algebraic kernel of T1.  The components
  \(\mathfrak o_{\mathrm{def}}\) and \(\mathfrak o_{\mathrm{RG}}\) of
  \(\mathfrak o_{\mathrm{BV,end}}\) measure failure of the brane-line
  extension of \(P^{\mathrm{base}}_{\epsilon,L}\) and of strict-window
  preservation of \(W(P_{\epsilon,L},-)\) inside this convention.
\end{rmk}

\subsection*{Costello--Li 2012 hypotheses, restated}

\begin{stmt}[Costello--Li 2012 BCOV construction,
   restated]\label{stmt:costello-li-2012-restated}
  Let \(X\) satisfy the compact Calabi-Yau holomorphic-volume
  hypotheses of \cite{costello-li-quantum-bcov}, with volume form
  \(\Omega\), \(\dim_{\C}X=d\).  Let
  \(\mathrm{PV}(X)[[t]]=\bigoplus_{i,j}\mathrm{PV}^{i,j}(X)[[t]]\) be
  the polyvector-field complex with the Tate parameter \(t\) for the
  worldsheet \(S^1\) rotation, BV differential
  \[
    Q_{\mathrm{BCOV}}=\bar\partial+t\,\partial_\Omega,\qquad
      |Q_{\mathrm{BCOV}}|=+1,
  \]
  with \(\partial_\Omega=\Omega^{-1}\circ\partial\circ\Omega\) the
  divergence operator, and the \((-1)\)-shifted symplectic pairing
  \[
    \langle\alpha,\beta\rangle
      =\int_X(\alpha\wedge\beta)\big|_{\mathrm{PV}^{d,d}}\cdot\Omega^2.
  \]
  The classical BCOV interaction \(I_{\mathrm{BCOV}}\) is the cubic
  vertex of \cite{costello-li-quantum-bcov}*{\S3}.  The cited
  construction supplies the generalized classical BCOV complex, its
  local-functional obstruction complex, and the renormalization
  framework of \cite{costello-renormalization} adapted to compact
  Calabi-Yau \(X\).  The 2015 open-closed sequel
  \cite{costello-li-open-closed-bcov} extends the construction to flat
  \(\C^d\) (\(d\) odd) coupled to open gauge data
  \(\lie{gl}(N\mid N)\), with the U(1)-anomaly cancellation
  \(\mathrm{str}(I)=N-N=0\).  Each is used here only under its stated
  geometric and coefficient hypotheses.
\end{stmt}

\subsection*{Hypothesis separation on \texorpdfstring{$\R^2\times\C^2$}{R2 x C2}
   with brane \texorpdfstring{$L=\R_{\mathrm{brane}}\times\{0\}\times\{p\}$}{L} and \texorpdfstring{$\lie{gl}_N$}{gl\_N}}

\begin{prop}[Costello--Li 2012/2015 hypothesis separation for the
   brane-defect endpoint]\label{prop:costello-li-2012-hypothesis-separation}
  Let \(\mathfrak o_{\mathrm{BV,end}}=(\mathfrak o_{\mathrm{Cas}},
  \mathfrak o_{\mathrm{def}},\mathfrak o_{\mathrm{RG}},
  \mathfrak o_{\mathrm{QME}})\) be the four-component endpoint
  obstruction class of \ref{defn:bv-endpoint-obstruction-tuple} for the
  formal local geometry
  \(X=\R^2_{\mathrm{top}}\times\widehat{\C^2}_{0,\mathrm{hol}}\) with
  brane \(L=\R_{\mathrm{brane}}\times\{0\}\times\{p\}\) and ordinary
  \(\lie{gl}_N\) gauge data.  The hypotheses of
  \ref{stmt:costello-li-2012-restated} differ from this local model
  along five independent axes.  In each axis the failure is named at
  the level of the cocycle representative the Costello--Li theorem does
  not control:
  \begin{enumerate}
    \item[(S1)] \emph{Compact Hodge-theoretic geometry vs.\ formal
      jet.}  The 2012 theorem requires compact Calabi-Yau \(X\) with
      Hodge-theoretic harmonic projection
      \(\Pi_{\mathrm{harm}}\colon\Omega^{0,\bullet}\to
      \mathcal H^{0,\bullet}\); on the formal jet
      \(\widehat{\C^2}_0\) there is no harmonic projector and
      \(H^*_{\mathrm{Dol}}(\widehat{\C^2}_0)=\C\) sits in degree zero
      only.  Hence the 2012 BV-cohomology vanishing
      \(H^*_{\mathrm{BV},\mathrm{BCOV}}(X)=0\) above degree zero is not
      a statement about
      \(\bigl(\mathcal O_{\mathrm{loc}}(\mathcal E_w),\Delta\bigr)\)
      on \(X=\R^2_{\mathrm{top}}\times\widehat{\C^2}_{0,\mathrm{hol}}\),
      and the cocycle representative
      of \(\mathfrak o_{\mathrm{QME}}\) is not its image.  The 2015
      open-closed theorem replaces compact \(X\) by flat \(\C^d\) (\(d\)
      odd) but keeps the holomorphic-volume kinematics and the
      \(\lie{gl}(N\mid N)\) anomaly cancellation; it is a different
      geometric and coefficient setting.
    \item[(S2)] \emph{Pure holomorphic-volume vs.\ mixed
      topological--holomorphic kinematics.}  The 2012 BV differential is
      \(Q_{\mathrm{BCOV}}=\bar\partial+t\,\partial_\Omega\) on
      \(\mathrm{PV}(X)[[t]]\).  The kinematic differential of the
      present model is
      \(D=d_{\R^2}+\bar\partial_{\C^2}\) on
      \(\Omega^\bullet_{\R^2}(I)\widehat\otimes
      \Omega^{0,\bullet}_{\C^2}\widehat\otimes
      (\mathfrak h\ltimes\mathfrak h^\vee_{\mathrm{cont}}[1])\),
      the holomorphic \(E_2\) factorization algebra on
      \(\C^2\).  The two complexes share neither generators nor
      differential, so the cocycle representative of any Costello--Li
      class on \((\mathrm{PV}(X)[[t]],Q_{\mathrm{BCOV}})\) is not
      transferable to a cocycle of \(\Delta\) on the mixed complex
      without a separate kinematic comparison map; none is supplied by
      the 2012/2015 theorems.  The protected sector of twisted
      M-theory \cite{costello-twistedM} carries this mixed kinematic
      shape as physical motivation.
    \item[(S3)] \emph{No brane vs.\ topological line defect.}  The 2012
      theorem has no brane sector; the propagator
      \(P^{\mathrm{2012}}_{\epsilon,L}\) on \(\mathrm{PV}(X)[[t]]\)
      carries no boundary condition along any submanifold.  The 2015
      theorem has an open-closed sector on flat \(\C^d\) with open gauge
      \(\lie{gl}(N\mid N)\), but the open boundary is the boundary of a
      worldsheet disk in \(\C^d\), not a transverse line
      \(L\subset\R^2_{\mathrm{top}}\times\C^2\) point-supported in the
      holomorphic direction.  Hence neither theorem extends
      \(P^{\mathrm{base}}_{\epsilon,L}\) across \(L\) with ordinary
      \(\lie{gl}_N\) brackets, and the obstruction
      \(\mathfrak o_{\mathrm{def}}\) of
      \ref{defn:bv-endpoint-obstruction-tuple} is uncontrolled.
    \item[(S4)] \emph{Supertrace cancellation vs.\ ordinary
      \(\lie{gl}_N\) scalar contact.}  On \(\lie{gl}(N\mid N)\) the
      \cite{costello-li-open-closed-bcov} cancellation
      \(\mathrm{str}(I)=N-N=0\) kills the would-be U(1)-anomaly at
      every loop order.  On ordinary \(\lie{gl}_N\) the same closed
      exchange supplies the scalar contact term
      \(\hbar N\omega(f,g)\gamma_{\mathbf 1}\) of
      \ref{thm:app-scalar-contact-qme-class}, with cohomology class
      \(\hbar N[\bar c]\in H^2_{\mathrm{Lie}}(\bar A,\C)\), equivalently
      the projective curvature of the modular line bundle
      \(\Omega_{\mathrm{central}}\) evaluated at the trace generator
      \(J(f)\) of Chapter~6
      (Theorem~\ref{thm:capelli-equals-modular-curvature}).
      This class is not killed by Costello--Li graph signs; it is the
      local trace-line avatar of the cancelled supertrace anomaly,
      not a graph-level identification with it.  Hence
      \(\mathfrak o_{\mathrm{QME}}\) carries the residual
      \(\hbar N[\bar c]\) summand for ordinary \(\lie{gl}_N\) at the
      U(1) centre-of-mass slice, and that summand is uncontrolled by
      either Costello--Li theorem in its stated form.
    \item[(S5)] \emph{Worldsheet \(t\)-Tate vs.\ coefficient-system
      Tate.}  The 2012 Tate parameter \(t\) is a worldsheet \(S^1\)
      rotation completion of the polyvector field complex.  The Tate
      direction required by
      Problem~\ref{prob:formal-factorization-center} is along the
      coefficient pair
      \[
        \widehat{\mathfrak h}\,\widehat\otimes\,
        \widehat{\mathfrak h}^{\vee}_{\mathrm{cont}},
        \qquad
        C_{\mathfrak h}=\sum_{d\ge1}\sum_ie_{d,i}\otimes e^i_d.
      \]
      By \ref{lem:tate-casimir-obstruction} the strict pair
      \((\mathfrak h_\Pi,D_\oplus)\) carries no represented kernel
      projecting to every finite Casimir
      \(C^M_{\mathfrak h}\): the dual side admits only finitely many
      degree components, while the compatible kernel has nonzero
      identity component in every \(H_d^\vee\).  Hence the cocycle
      representative
      \(\mathfrak o_{\mathrm{Cas}}=[(C^M_{\mathfrak h})_M]\) is nonzero
      in the cokernel.  The 2012/2015 theorems are silent on this
      coefficient direction; their \(t\)-Tate completion is orthogonal
      to it.
  \end{enumerate}

  Each axis identifies a distinct uncontrolled component of
  \(\mathfrak o_{\mathrm{BV,end}}\): (S1)+(S2) prevent any direct
  identification of \(\mathfrak o_{\mathrm{QME}}\) with a Costello--Li
  cocycle; (S3) prevents control of \(\mathfrak o_{\mathrm{def}}\);
  (S4) names the residual \(\hbar N[\bar c]\) summand of
  \(\mathfrak o_{\mathrm{QME}}\); (S5) names the cokernel class of
  \(\mathfrak o_{\mathrm{Cas}}\).  The remaining component
  \(\mathfrak o_{\mathrm{RG}}\) is the failure of the strict
  finite-window Mittag-Leffler tower to be preserved by the recursion
  \(W(P_{\epsilon,L},I)\) of \ref{rmk:t4-costello-renorm-datum}; this
  preservation lies outside the surface of any compact-target
  Costello--Li theorem.
\end{prop}

\begin{proof}
  Each axis is a direct comparison of the hypotheses of
  \ref{stmt:costello-li-2012-restated} (and its 2015 sequel) with the
  data of \ref{thm:main-local}, the linear-heat / Casimir analysis
  (\ref{rmk:linear-heat-versus-bv-kernel},
  \ref{lem:tate-casimir-obstruction}), the scalar contact theorem
  \ref{thm:app-scalar-contact-qme-class}, and the strict-window category
  \ref{defn:strict-ml-tate-habitat}.

  \emph{(S1).}  Compactness enters \cite{costello-li-quantum-bcov}
  through the Hodge-theoretic harmonic projection used to construct
  the BV propagator and to verify the QME at the parametrix limit
  \(\epsilon\to0\), \(L\to\infty\); on the formal jet
  \(\widehat{\C^2}_0\) this projection has no analogue and
  \(H^*_{\mathrm{Dol}}(\widehat{\C^2}_0)\) is one-dimensional.

  \emph{(S2).}  The 2012 BV differential is purely Dolbeault plus the
  divergence \(t\,\partial_\Omega\); the present
  \(D=d_{\R^2}+\bar\partial_{\C^2}\) carries an additional de Rham
  factor on the topological line.  No symmetric-monoidal map
  \((\mathrm{PV}(X)[[t]],Q_{\mathrm{BCOV}})\to(\Omega^\bullet_{\R^2}
  (I)\widehat\otimes\Omega^{0,\bullet}_{\C^2}\widehat\otimes\cdots,D)\)
  is supplied by the cited theorems.

  \emph{(S3).}  The brane \(L\) is transverse to the topological
  factor and point-supported in the holomorphic factor; the open
  boundary in \cite{costello-li-open-closed-bcov} is a worldsheet
  boundary on flat \(\C^d\), not a Wilson line on a transverse
  topological direction.  The bulk Hadamard parametrix
  \(P^{\mathrm{base}}_{\epsilon,L}\) of
  \cite{costello-renormalization}*{Ch.~3} on
  \(\R^2\times\C^2_{\mathrm{hol}}\) does not by itself decide the
  brane-line trace.

  \emph{(S4).}  By \ref{thm:app-scalar-contact-qme-class} the closed
  exchange between two Hamiltonian sources
  \(B_f,B_g\) on the brane line produces the universal scalar contact
  density
  \(\hbar N\,\omega(f,g)\,\gamma_{\mathbf 1}(t)\,a(t)b(t)\).  Its
  associated-graded scalar symbol is the Lie 2-cocycle
  \(\omega(f,g)\,\gamma_{\mathbf 1}\) on \(\bar A\), with
  \(\omega(z_1,z_2)=1\) by
  \ref{cor:app-sign-universal-dual-normalization}; this is a nonzero
  class in
  \(H^2_{\mathrm{Lie}}(\bar A,\C)\) by \ref{thm:main-local}~part~(f).
  On \(\lie{gl}(N\mid N)\) the same scalar is multiplied by
  \(\mathrm{sdim}(\C^{N|N})=N-N=0\), killing the representative.  On
  ordinary \(\lie{gl}_N\) it is \(\hbar N\), nonzero, and the
  Costello--Li sequel does not supply a counterterm absorbing it.

  \emph{(S5).}  The compatible Casimir family
  \((C^M_{\mathfrak h})_M\) projects nonzero into every
  \(H_d\otimes H_d^\vee\) for \(d\le M\), while the strict direct-sum
  dual \(D_\oplus=\bigoplus_dH_d^\vee\) has only finitely many nonzero
  degree components per element.  Hence
  \([C_{\mathfrak h}]\not\in\mathrm{im}(\mathfrak h_\Pi\widehat\otimes
  D_\oplus\to(\mathfrak h_\Pi\widehat\otimes D_\oplus)^{\wedge\bullet})\)
  by \ref{lem:tate-casimir-obstruction}; equivalently the cocycle
  representative of
  \(\mathfrak o_{\mathrm{Cas}}\) is nonzero, and the worldsheet
  \(t\)-Tate completion of \cite{costello-li-quantum-bcov} commutes
  with this coefficient direction trivially.

  Finally, \(\mathfrak o_{\mathrm{RG}}\) is the assertion that
  \(W(P_{\epsilon,L},-)\) sends the strict ML category
  \(\Cat{MLTateChain}_{\geq0}^{\mathrm{str}}\) of
  \ref{defn:strict-ml-tate-habitat} into itself with surjective
  finite-window transition maps; this is a statement about the
  algebraic-coefficient direction of the propagator, not the
  worldsheet direction, and is not part of either Costello--Li
  theorem.
\end{proof}

\begin{cor}[Costello--Li input and the unreduced coefficient endpoint]
\label{cor:costello-li-input-coefficient-endpoint}
  The Costello--Li 2012 BCOV construction supplies a pure closed BCOV
  template; the 2015 sequel supplies the open-closed extension on flat
  \(\C^d\) with \(\lie{gl}(N\mid N)\) gauge data.  Neither, in the
  form cited, supplies vanishing of any of the four cocycle classes
  in \(\mathfrak o_{\mathrm{BV,end}}\) for the present
  \(\R^2_{\mathrm{top}}\times\widehat{\C^2}_{0,\mathrm{hol}}\)
  brane-defect endpoint.  The unreduced Tate-coefficient cotangent
  lift therefore additionally requires the endpoint coefficient data
  of
  Definition~\ref{defn:endpoint-admissible-coefficient-module}.  The
  classical primitive indecomposable shadow on the open observables is
  closed without analytic input by
  \ref{thm:chain-level-primitive-projection}; the admissible Tate
  envelope on which the obstruction tuple is computed is provided by
  the model-categorical envelope of \ref{stmt:tate-model-envelope}.
  The unreduced cotangent factorization-centre morphism and the
  quantum one-antifield extension of part~(e) of \ref{thm:main-local}
  require the weighted coefficient category of T1 and the mixed
  brane-defect locality/QME input of
  Problem~\ref{prob:weighted-rg-locality} together with
  \ref{thm:hadamard-mittag-leffler}.
\end{cor}

\begin{proof}
  By
  Proposition~\ref{prop:costello-li-2012-hypothesis-separation}, each
  Costello--Li hypothesis is silent on a distinct cocycle representative
  in \(\mathfrak o_{\mathrm{BV,end}}\); vanishing of the tuple is
  not a consequence of the cited theorems.  The remaining clauses are
  the classical chain-level lift of
  \ref{thm:chain-level-primitive-projection}, the model-categorical
  envelope of \ref{stmt:tate-model-envelope}, and the weighted
  finite-scale coefficient kernel of T1; the endpoint data are exactly
  those listed in
  Definition~\ref{defn:endpoint-admissible-coefficient-module}.
\end{proof}

\begin{thm}[Costello--Li endpoint coefficient criterion]
\label{thm:costello-li-no-coefficient-endpoint}
  The Costello--Li 2012 BCOV construction
  \cite{costello-li-quantum-bcov} and its 2015 open-closed extension
  \cite{costello-li-open-closed-bcov}, in the forms restated in
  \ref{stmt:costello-li-2012-restated}, supply an endpoint-admissible
  coefficient module of
  Definition~\ref{defn:endpoint-admissible-coefficient-module} for the
  formal local brane-defect geometry only after three independent
  classical data are added:
  \[
    \text{(E2) coefficient Casimir kernel},\quad
    \text{(E3) mixed brane-defect transport},\quad
    \text{(E5) endpoint RG preservation},
  \]
  with (E1) and (E4) the inherited finite-window and Mittag-Leffler
  envelope conditions.  A quantum endpoint additionally requires
  vanishing of the QME component
  \(\mathfrak o_{\mathrm{QME}}\) of
  Definition~\ref{defn:bv-endpoint-obstruction-tuple}.  Equivalently:
  none of the four cocycle representatives in
  \(\mathfrak o_{\mathrm{BV,end}}\) is required to vanish by either
  Costello--Li theorem on this geometry.
\end{thm}

\begin{proof}
  An endpoint-admissible coefficient module requires a coefficient
  kernel
  \(C_\ast\in\mathfrak h_\ast\widehat\otimes D_\ast\) whose projection
  to every finite Hamiltonian window is the finite Casimir
  \(C^M_{\mathfrak h}\), together with mixed brane-defect transport of
  the bracket, propagator, interaction, counterterms, and RG recursion
  (Definition~\ref{defn:endpoint-admissible-coefficient-module}).  By
  Proposition~\ref{prop:costello-li-2012-hypothesis-separation}, each
  axis (S1)--(S5) names a distinct cocycle in
  \(\mathfrak o_{\mathrm{BV,end}}\) that the Costello--Li hypotheses
  do not control: (S5) for \(\mathfrak o_{\mathrm{Cas}}\), (S3) for
  \(\mathfrak o_{\mathrm{def}}\), the algebraic-coefficient direction
  for \(\mathfrak o_{\mathrm{RG}}\), and (S1)+(S2)+(S4) for
  \(\mathfrak o_{\mathrm{QME}}\).  Hence (E2), (E3), (E5), and the QME
  vanishing are not consequences of the cited theorems.
\end{proof}

\begin{defn}[BV endpoint obstruction tuple, with explicit cocycle
   representatives]
\label{defn:bv-endpoint-obstruction-tuple}
  For the formal mixed local geometry
  \(X=\R^2_{\mathrm{top}}\times\widehat{\C^2}_{0,\mathrm{hol}}\) with
  brane \(L=\R_{\mathrm{brane}}\times\{0\}\times\{p\}\) and ordinary
  \(\lie{gl}_N\) gauge data, the BV endpoint obstruction tuple is
  \[
    \mathfrak o_{\mathrm{BV,end}}
      =\bigl(
        \mathfrak o_{\mathrm{Cas}},\,
        \mathfrak o_{\mathrm{def}},\,
        \mathfrak o_{\mathrm{RG}},\,
        \mathfrak o_{\mathrm{QME}}
       \bigr).
  \]
  Each component is named with its ambient cohomology, BV degree,
  and explicit cocycle representative:
  \begin{enumerate}[label=(O\arabic*)]
    \item \emph{Casimir cokernel class
      \(\mathfrak o_{\mathrm{Cas}}\)} --- in
      \(\mathrm{coker}\bigl(\mathfrak h_\Pi\widehat\otimes D_\oplus
      \to\varprojlim_M(\mathfrak h\otimes\mathfrak h^\vee)_{\le M}\bigr)\),
      degree \(0\); cocycle representative the compatible family
      \[
        \mathfrak o_{\mathrm{Cas}}=
        \bigl[(C^M_{\mathfrak h})_{M\ge1}\bigr],
        \qquad
        C^M_{\mathfrak h}=\sum_{d\le M}\sum_ie_{d,i}\otimes e^i_d,
      \]
      with transition maps the standard truncations
      \(C^{M+1}_{\mathfrak h}\mapsto C^M_{\mathfrak h}\); nonzero in
      the strict pair by \ref{lem:tate-casimir-obstruction}.
    \item \emph{Brane-defect transport class
      \(\mathfrak o_{\mathrm{def}}\)} --- in
      \(H^0\!\bigl(\mathrm{Hom}^\bullet_{\mathrm{loc}}
        (P^{\mathrm{base}}_{\epsilon,L},
         P^{\mathrm{brane}}_{\epsilon,L}),\,\delta_L\bigr)\),
      degree \(0\); cocycle representative the brane-line restriction
      datum
      \[
        \mathfrak o_{\mathrm{def}}=
        \bigl[\delta_L^*P^{\mathrm{base}}_{\epsilon,L}
          -P^{\mathrm{brane}}_{\epsilon,L}\bigr]
      \]
      together with the failure of bracket transport
      \(\{\,\cdot\,,\,\cdot\,\}_{\mathrm{open}}\) of
      \ref{lem:app-sign-current-source} across \(L\) with ordinary
      \(\lie{gl}_N\) brackets, equivalently the Hadamard locality
      class of \ref{thm:hadamard-mittag-leffler}~item~(2).
    \item \emph{RG-window preservation class
      \(\mathfrak o_{\mathrm{RG}}\)} --- in the Mittag-Leffler
      \(\varprojlim^1H^0\) of the strict-window functional complex
      \(\Cat{MLTateChain}_{\geq0}^{\mathrm{str}}\) of
      \ref{defn:strict-ml-tate-habitat}, degree \(0\); cocycle
      representative the compatible family of recursion-induced
      transition failures
      \[
        \mathfrak o_{\mathrm{RG}}
        =\bigl[
          \mathrm{T}^{M,M-1}\circ
          W(P_{\epsilon,L},-)|_{\le M}
          -W(P_{\epsilon,L},-)|_{\le M-1}\circ\mathrm{T}^{M,M-1}
        \bigr]
        \in\varprojlim\nolimits^1_M H^0,
      \]
      where \(\mathrm{T}^{M,M-1}\) is the strict ML truncation of
      \ref{defn:strict-ml-tate-habitat} and
      \(W(P_{\epsilon,L},-)\) is the Costello recursion of
      \ref{rmk:t4-costello-renorm-datum}.
    \item \emph{QME anomaly class
      \(\mathfrak o_{\mathrm{QME}}\)} --- in the local-functional
      cohomology
      \[
        H^1\!\bigl(\mathcal O_{\mathrm{loc}}(\mathcal E_w),\,
          \Delta\bigr),
        \qquad
        \Delta=Q+\{I_0,-\},\ \ |\Delta|=+1,
      \]
      degree \(+1\); cocycle representative the local functional
      \[
        \mathfrak o_{\mathrm{QME}}
          =\bigl[QI_0+\hbar\Delta_LI_0
                 +\tfrac12\{I_0,I_0\}_L\bigr]
      \]
      produced by the quantum master equation of
      \cite{costello-renormalization}*{Ch.~5} at scale \(L\),
      restricted to the mixed brane-defect interaction \(I_0\) of
      Problem~\ref{prob:weighted-rg-locality}.  In the ordinary
      \(\lie{gl}_N\) branch this class carries the residual scalar
      contact summand
      \(\hbar N\,\omega(f,g)\,\gamma_{\mathbf 1}\) of
      \ref{thm:app-scalar-contact-qme-class}, whose scalar projection
      at the U(1) centre-of-mass slice is the Capelli class
      \(\hbar N[\bar c]\) of Chapter~6
      (Theorem~\ref{thm:capelli-equals-modular-curvature}); in the
      balanced \(\C^{N|N}\) branch the same summand vanishes by
      \ref{thm:app-balanced-supertrace-qme-cancellation}.  The scalar
      projection is therefore the local-stalk value of the projective
      curvature \(\Omega_{\mathrm{central}}\big|_{J(f)}\) of the
      modular line bundle of the closed chiral algebra, by
      Theorem~\ref{thm:capelli-equals-modular-curvature}.
  \end{enumerate}
\end{defn}

\begin{prop}[Endpoint construction is equivalent to vanishing of the
   tuple]
\label{prop:bv-endpoint-obstruction-tuple}
  An endpoint-admissible coefficient module of
  Definition~\ref{defn:endpoint-admissible-coefficient-module} for the
  brane-defect geometry of
  \ref{defn:bv-endpoint-obstruction-tuple} exists if and only if all
  four cocycle classes
  \(\mathfrak o_{\mathrm{Cas}}\), \(\mathfrak o_{\mathrm{def}}\),
  \(\mathfrak o_{\mathrm{RG}}\), \(\mathfrak o_{\mathrm{QME}}\) of
  \ref{defn:bv-endpoint-obstruction-tuple} vanish and a compatible
  choice of primitives is made.  In particular, a Costello--Li type
  BV-cohomology theorem closes the present coefficient endpoint only
  when it proves vanishing of all four classes simultaneously, with
  primitives compatible with the strict finite-window Mittag-Leffler
  envelope of \ref{defn:strict-ml-tate-habitat}.
\end{prop}

\begin{proof}
  \emph{(O1)\(\Leftrightarrow\)(E2).}
  Definition~\ref{defn:endpoint-admissible-coefficient-module}(E2) is
  the existence of a coefficient kernel
  \(C_*\in\mathfrak h_*\widehat\otimes D_*\) whose finite-window
  projections are the \(C^M_{\mathfrak h}\); equivalently a primitive
  for the cocycle representative of
  \(\mathfrak o_{\mathrm{Cas}}\) under the represented-kernel map.

  \emph{(O2)\(\Leftrightarrow\)(E3).}  Condition (E3) requires
  transport of bracket
  \(\{\,\cdot\,,\,\cdot\,\}_{\mathrm{open}}\), propagator
  \(P^{\mathrm{base}}_{\epsilon,L}\), interaction \(I_0\), and
  counterterm representatives across the brane line \(L\) with ordinary
  \(\lie{gl}_N\) brackets; equivalently a primitive for the
  Hom-complex cocycle representative of \(\mathfrak o_{\mathrm{def}}\).

  \emph{(O3)\(\Leftrightarrow\)(E5).}  Condition (E5) is preservation
  of the strict finite-window Mittag-Leffler tower under the Costello
  recursion \(W(P_{\epsilon,L},-)\); equivalently vanishing of the
  \(\varprojlim^1H^0\) class of
  \(\mathfrak o_{\mathrm{RG}}\).

  \emph{(O4) and the quantum endpoint.}  The endpoint QME is the
  vanishing of the degree-\(+1\) local-functional class
  \(\mathfrak o_{\mathrm{QME}}\) in
  \(H^1\bigl(\mathcal O_{\mathrm{loc}}(\mathcal E_w),\Delta\bigr)\),
  which by \ref{def:app-unreduced-bv-degrees} has \(|\Delta|=+1\) on
  the cotangent-coordinate grading of the local-functional complex.

  Necessity is the same chain of equivalences in reverse: a nonzero
  component removes one of the endpoint data just listed.  The
  primitive-compatibility clause is the requirement that the chosen
  cobounding primitives be morphisms in the strict ML category
  \(\Cat{MLTateChain}_{\geq0}^{\mathrm{str}}\), so that the endpoint
  module sits in
  \(\Cat{TateChain}_{\geq0}^{\mathrm{adm}}\) (\ref{stmt:tate-model-envelope}).
\end{proof}

\subsection*{Four-input extension criterion}

\begin{thm}[Costello--Li extension criterion for the mixed coefficient
   endpoint]\label{thm:t4-four-missing}
  Let \(\mathfrak o_{\mathrm{BV,end}}\) be the obstruction tuple of
  Definition~\ref{defn:bv-endpoint-obstruction-tuple}.  A Costello--Li
  type BV-cohomology theorem proves vanishing of \(\mathfrak
  o_{\mathrm{BV,end}}\) on the formal local geometry
  \(X=\R^2_{\mathrm{top}}\times\widehat{\C^2}_{0,\mathrm{hol}}\), brane
  \(L=\R_{\mathrm{brane}}\times\{0\}\times\{p\}\), gauge \(\lie{gl}_N\),
  if and only if it supplies the following four classical and one
  quantum data simultaneously, each addressing a distinct cocycle
  representative in
  \(\mathfrak o_{\mathrm{BV,end}}\):
  \begin{enumerate}
    \item[\emph{(I1)}] \emph{Non-compact / \(\Omega\)-background
      BV-cohomology vanishing} replacing (S1).  A vanishing theorem
      for the BV operator \(\Delta\) on
      \(\bigl(\mathcal O_{\mathrm{loc}}(\mathcal E_w),\Delta\bigr)\)
      over the non-compact base
      \(\R^2_{\mathrm{top}}\times\C^2_{\mathrm{hol}}\), either via a
      separately anchored \(\Omega\)-background route in the normal
      bundle \(N_LX=\R_s\oplus\C_{z_1}\oplus\C_{z_2}\), with the
      vector field, equivariant differential
      \(Q_\Omega=Q+\iota_{V_\Omega}\), \(Q_\Omega^2=L_{V_\Omega}\)
      mechanism, inverted normal weights, residue/Euler normalization,
      and stratified factorization on \((X,L)\), or via
      compactification with Lagrangian boundary at infinity preserving
      the brane \(L\) as a fixed locus.
    \item[\emph{(I2)}] \emph{Topological--holomorphic mixed kinematics}
      replacing (S2).  The vanishing must hold for
      \(D=d_{\R^2}+\bar\partial_{\C^2}\) on
      \(\Omega^\bullet_{\R^2}(I)\widehat\otimes
        \Omega^{0,\bullet}_{\C^2}\widehat\otimes
        (\mathfrak h\ltimes\mathfrak h^\vee_{\mathrm{cont}}[1])\),
      together with its linearized BV-kernel complex of
      \ref{rmk:linear-heat-versus-bv-kernel} and the cotangent
      \([1]\)-extension of the Hamiltonian sector
      (\ref{def:app-unreduced-bv-degrees}).
    \item[\emph{(I3)}] \emph{Brane-defect extension with ordinary
      \(\lie{gl}_N\) scalar contact decision} replacing (S3)--(S4).
      The vanishing must allow the transverse one-real-dimensional
      defect \(L\) carrying open gauge \(\lie{gl}_N\) (not the
      flat-\(\C^d\) \(\lie{gl}(N\mid N)\) setup of
      \cite{costello-li-open-closed-bcov}), and must decide the scalar
      contact class
      \[
        \hbar N\,\omega(f,g)\,\gamma_{\mathbf 1}\in
        H^2_{\mathrm{Lie}}(\bar A,\C),
        \qquad\omega(z_1,z_2)=1,
      \]
      of \ref{thm:app-scalar-contact-qme-class} --- equivalently the
      projective curvature
      \(\Omega_{\mathrm{central}}\big|_{J(f)}\) of the modular line
      bundle at the trace generator
      (Theorem~\ref{thm:capelli-equals-modular-curvature}) --- by one
      of the four named cancellations: closed-exchange cancellation,
      \(\C^{N|N}\) supertrace removal
      (\ref{thm:app-balanced-supertrace-qme-cancellation}),
      central-character normalization
      (\ref{thm:app-central-character-qme-cancellation}), or
      renormalized boundary generators.
    \item[\emph{(I4)}] \emph{Coefficient-system Tate completion}
      replacing (S5).  The coefficient direction
      \(\widehat{\mathfrak h}\,\widehat\otimes\,
        \widehat{\mathfrak h}^{\vee}_{\mathrm{cont}}\)
      must support the Casimir
      \(C_{\mathfrak h}=\sum_{d\ge1}\sum_ie_{d,i}\otimes e^i_d\)
      as a convergent represented kernel; by
      \ref{lem:tate-casimir-obstruction} this requires a coefficient
      category change, supplied for example by the weighted Tate pair
      \((\mathfrak h_w,\mathfrak h^{\vee,w}_{\mathrm{cont}})\) of T1.
    \item[\emph{(I5)}] \emph{Quantum endpoint vanishing}.  A primitive
      for the degree-\(+1\) cocycle
      \(\mathfrak o_{\mathrm{QME}}=
        \bigl[QI_0+\hbar\Delta_LI_0+\tfrac12\{I_0,I_0\}_L\bigr]\)
      in \(H^1\!\bigl(\mathcal O_{\mathrm{loc}}(\mathcal E_w),
        \Delta\bigr)\), with the Costello-2011 counterterm subtraction
      of \ref{rmk:t4-costello-renorm-datum} compatible with the
      strict-window Mittag-Leffler tower of
      \ref{defn:strict-ml-tate-habitat}.  Casimir-balanced equivariant
      data of
      \ref{defn:super-balanced-equivariant-qme-datum} kill the scalar
      component
      \(\mathfrak o_{\mathrm{QME}}^{\mathrm{sc}}\); the non-scalar
      residual is the obstruction class of
      \ref{thm:wt-nonscalar-counterterm-criterion} in
      \(H^1(\ker\widehat\sigma_{\mathrm{sc}},d_{\mathrm{QME}})\)
      together with the secondary
      \(\varprojlim^1H^0\) primitive class.
  \end{enumerate}
  Equivalently, (I1)--(I5) are exactly the data of
  Problem~\ref{prob:formal-factorization-center} for the present
  brane-defect endpoint.
\end{thm}

\begin{proof}
  By Proposition~\ref{prop:costello-li-2012-hypothesis-separation},
  axes (S1)--(S5) name pairwise distinct cocycle representatives in
  \(\mathfrak o_{\mathrm{BV,end}}\) that the Costello--Li hypotheses do
  not control: (S1)+(S2) for the kinematic side of
  \(\mathfrak o_{\mathrm{QME}}\), (S3) for
  \(\mathfrak o_{\mathrm{def}}\), (S4) for the residual
  \(\hbar N[\bar c]\) summand of \(\mathfrak o_{\mathrm{QME}}\), and
  (S5) for \(\mathfrak o_{\mathrm{Cas}}\); the
  algebraic-coefficient direction governs
  \(\mathfrak o_{\mathrm{RG}}\).  Components (I1)--(I4) supply
  primitives, in turn, for the cocycle representatives forced by
  (S1)+(S2), (S3)+(S4)\(_{\mathrm{transport}}\), the strict-window
  Mittag-Leffler preservation of \(W(P_{\epsilon,L},-)\), and the
  Casimir convergence; (I5) supplies the quantum primitive.  By
  Proposition~\ref{prop:bv-endpoint-obstruction-tuple} this is exactly
  vanishing of \(\mathfrak o_{\mathrm{BV,end}}\), with primitives
  compatible with
  \(\Cat{MLTateChain}_{\geq0}^{\mathrm{str}}\) of
  \ref{defn:strict-ml-tate-habitat}.

  For necessity: dropping (I1) leaves the kinematic representative of
  \(\mathfrak o_{\mathrm{QME}}\) unprimitivated; dropping (I2) leaves
  \(D\) without a vanishing theorem; dropping (I3) leaves
  \(\mathfrak o_{\mathrm{def}}\) and the scalar contact summand of
  \(\mathfrak o_{\mathrm{QME}}\) unaddressed; dropping (I4) leaves
  \(\mathfrak o_{\mathrm{Cas}}\) nonzero in the cokernel by
  \ref{lem:tate-casimir-obstruction}; dropping (I5) leaves the
  degree-\(+1\) anomaly primitive unsupplied.  The chain-level
  primitive shadow of \ref{thm:chain-level-primitive-projection}
  closes the classical indecomposable shadow without analytic input
  but does not by itself supply (I3) or (I5); the Quillen envelope of
  \ref{thm:tate-bar-cobar-quillen} promotes the strict category of
  (I4) to an admissible Tate model envelope.
\end{proof}

